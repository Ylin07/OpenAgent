向本地文件系统写入文件。

用法：
- 如果提供路径已有文件，此工具会覆盖现有文件。
- 如果这是现有文件，你必须先使用 Read 工具读取文件内容。若未先读取文件，此工具会失败。
- 始终优先编辑代码库中的现有文件。除非明确需要，否则绝不要写入新文件。
- 绝不要主动创建文档文件（*.md）或 README 文件。只有在用户明确要求时才创建文档文件。
- 只有在用户明确要求时才使用 emojis。除非被要求，否则避免向文件写入 emojis。
