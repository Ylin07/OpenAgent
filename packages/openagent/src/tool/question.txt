执行过程中需要向用户提问时使用此工具。它允许你：
1. 收集用户偏好或需求
2. 澄清模糊指令
3. 在工作过程中获取实现选择的决策
4. 向用户提供可选方向。

用法说明：
- 当 `custom` 启用（默认）时，会自动添加“输入自定义答案”选项；不要包含“其他”或兜底选项
- 答案会以 labels 数组返回；设置 `multiple: true` 可允许选择多个
- 如果推荐某个特定选项，将它放在列表第一位，并在 label 末尾添加“（推荐）”
